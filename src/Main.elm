module Main exposing (main)

import Html exposing (h1, text)



{- Changes to this Elm file are only detected by Parcel once, after which
   changes get ignored
-}


main =
    h1 [] [ text "Hello, world" ]
