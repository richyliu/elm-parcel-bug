/*
 * Commenting out this line and then uncommenting this line forces Parcel to
 * compile the Elm file
 */
import { Elm } from './Main.elm';

Elm.Main.init({
  node: document.querySelector('main')
});
